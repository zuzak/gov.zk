//- Used for testing: [squeamish ossifrage]
extends ../skeleton
blokc title
  title Add to Longlist
block breadcrumb
  li
    a(href='/book-club') Book club
  li
    a(href='/book-club/long-list') Long list
block content
  .grid-row
    .column-two-thirds
      h1.heading-xlarge Add a book
      form(method='POST')
        h2.heading-medium Please add some basic details about the book.
        .form-group
          label.form-label(for='book-title') Book title
          input.form-control#book-title(type='text',name='title',required)
        .form-group
          label.form-label(for='book-author') Book author
          input.form-control#book-author(type='text',name='author',required)
        .form-group
          label.form-label(for='book-isbn')
            span International Standard Book Number
          input.form-control#book-isbn(type='number',name='isbn',maxlength="13",required)
        .form-group
          .multiple-choice
            input#difficult(name='difficult',type='checkbox')
            label(for='difficult') This book is very long or difficult

        .form-group
          input.button(type='submit',value='Add book to long list')
  a.link-back(href='/book-club/long-list') Return to long list

